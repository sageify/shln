# Default nvrc to add $HOME/bin to path and default environment profile

# Behaves like environment files except variables 
# are expanded.  To prevent variable expansion, 
# use /$ instaed of $

# path to lnkn files 
PATH=$HOME/bin:$PATH

# select default environment profile
ENVY_PROFILE=nv
